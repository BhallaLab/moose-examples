sudo : required 
language: python
python:
    - "3.6"
    - "nightly"

services:
    - xvfb 

addons:
    apt:
        update: true

notifications:
    email:
        recipients:
            - dilawar.s.rajput@gmail.com
            - upi.bhalla@gmail.com
            - hrani@ncbs.res.in


script:
    - # We use python3 to run the travis scripts. Which requires subprocess32 modules.
    - echo $(which python)
    - pip wheel pymoose --pre --upgrade
    - pip wheel -r .travis/requirements.txt 
    -  $(which python) ./.travis/execute_travis_serial.py --python $(which python) -n 60

