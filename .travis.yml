sudo : required 
language: python
python:
    - "2.7"
    - "3.6"
    - "3.7-dev"

notifications:
    email:
        recipients:
            - dilawar.s.rajput@gmail.com

script:
    - pip install pymoose --pre
    - pip install subprocess32 --upgrade
    - pip install libneuroml pyneuroml scipy matplotlib numpy --upgrade
    - cd ./.travis && ./execute_travis.py

exclude: [vendor]
