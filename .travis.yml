sudo : required 
language: python
python:
    - "2.7"
    - "3.6"

notifications:
    email:
        recipients:
            - dilawar.s.rajput@gmail.com
            - bhalla@ncbs.res.in
            - hrani@ncbs.res.in

script:
    - pip install pymoose numpy --pre
    - pip install matplotlib scipy python-libsbml pyneuroml --upgrade
    - pip install pyqt5 || echo "No pyqt5 for this version"
    - ./.travis_run.sh

exclude: [vendor]
