sudo : required 
language: python
python:
    - "3.6"
    - "nightly"

addons:
    apt:
        update: true

notifications:
    email:
        recipients:
            - dilawar.s.rajput@gmail.com
            - upi.bhalla@gmail.com
            - hrani@ncbs.res.in

before_script:
    - "export DISPLAY=:99.0"
    - "sh -e /etc/init.d/xvfb start"
    - sleep 2

script:
    - # We use python3 to run the travis scripts. Which requires subprocess32 modules.
    - pip install pymoose --pre --no-deps
    - pip install pylint subprocess32
    - pip install numpy --upgrade
    - pip install subprocess32
    - pip install networkx 
    - pip install brian2 
    - pip install libneuroml pyneuroml scipy matplotlib
    - pip install python-libsbml 
    -  $(which python) ./.travis/execute_travis_serial.py `which python`

