sudo : required 
dist: bionic
services:
    - xvfb 

language: python
python:
    - "3.6"
    - "nightly"

addons:
    apt:
        update: true

notifications:
    email:
        recipients:
            - dilawar.s.rajput@gmail.com
            - upi.bhalla@gmail.com
            - hrani@ncbs.res.in

script:
    - # We use python3 to run the travis scripts. Which requires subprocess32 modules.
    - sudo apt install -y python3-pip python3-dev python3-matplotlib python3-scipy
    - sudo apt install -y graphviz python3-networkx
    - echo $(which python)
    - pip3 wheel pymoose --pre --upgrade
    - pip3 wheel -r .travis/requirements.txt 
    -  $(which python) ./.travis/execute_travis_serial.py --python $(which python) -n 60

