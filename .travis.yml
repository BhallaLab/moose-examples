sudo : required 
language: python
python:
    - "2.7"
    - "3.6"
    - "3.7-dev"

notifications:
    email:
        recipients:
            - dilawar.s.rajput@gmail.com
            - upi.bhalla@gmail.com
            - hrani@ncbs.res.in

before_script:
    - "export DISPLAY=:99.0"
    - "sh -e /etc/init.d/xvfb start"
    - sleep 2
    - sudo apt install -yy python3-pip

script:
    - # We use python3 to run the travis scripts. Which requires subprocess32 modules.
    - /usr/bin/python3 -m pip install subprocess32 --user
    - pip install pymoose --pre --no-deps
    - pip install subprocess32
    - pip install networkx 
    - pip install libneuroml pyneuroml scipy matplotlib numpy 
    - ./.travis/execute_travis_serial.py
